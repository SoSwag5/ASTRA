# SLSA v1.2 release provenance assessment
Authority: https://slsa.dev/spec/v1.2/build-requirements and https://slsa.dev/spec/v1.2/provenance . Assessment is against the approved v1.2 Build track. **No SLSA Build level is claimed. No signed provenance exists yet.**

| Requirement | Prepared implementation | Objective evidence still required |
|---|---|---|
| Appropriate build platform | GitHub-hosted Windows release job | Actual run and runner identity; platform assumptions reviewed |
| Consistent build process | Pinned workflow, hash-locked dependencies, build_release.py, manifest | Same source SHA through verification/build/attestation |
| Distribute provenance | actions/attest; preserve bundles and verified release evidence | Public artifact and retrievable attestation availability |
| Provenance exists | ZIP subject SHA-256 generated by action | Signed statement identifying artifact, source, builder and invocation |
| Authentic provenance | OIDC/Sigstore GitHub attestation, no user signing key | Independent gh attestation verify tied to repo, workflow and source digest; inspect actual predicate |
| Hosted execution | All release build steps run on GitHub-hosted runner | Run URL, commit, hosted environment, actual artifacts |
| Security best practices | Least privilege, SHA pins, no PR secret grants | Enabled repository rules/scanning; reviewed platform and dependency controls |

Build L2 also depends on the platform-controlled provenance generation and anti-tamper requirement, not just a signature. Inspect actual predicate fields and trust boundary against v1.2; identify any tenant-supplied fields. Do not assume GitHub's SLSA predicate version number alone establishes the v1.2 requirements. Build L3 isolation/unforgeability is outside the current target.

After authorized remote execution: download the build's exact artifact, independently run gh verification with --repo, --signer-workflow and --source-digest; verify the CycloneDX predicate separately, record attestation identity and compare embedded BOM with its recorded digest. Archive the full verification outputs privately/release-safely. Only if every L2 requirement has objective evidence may wording become 'SLSA v1.2 Build L2 requirements assessed as satisfied'. Otherwise use 'SLSA v1.2-aligned signed build provenance' only after signed provenance actually exists and verifies. Today: 'SLSA v1.2-aligned provenance workflow prepared; hosted execution and verification pending.'
